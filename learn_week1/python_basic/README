This is basic sample about python
Modules:
- Web page and APIs using Flask
- Web Page and APIs using FastAPI
- some excersises about python data type

Prerequisites
Python 3.12.x (recommended for compatibility)

I, Web Application
 - Activate the virtual environment (located at project root):
```# From the project root directory
.\myenv\Scripts\Activate.ps1```
 - Install dependencies (if not already installed):
`pip install -r requirements.txt`
 - Start the development server: 
 + Web page and APIs using Flask: 
  * `python .\web_flask\app.py`
  * The web application will be available at http://127.0.0.1:5050
 + Web Page and APIs using FastAPI: 
  * `uvicorn webapi_fastapi.main:app --reload --port 8080`
  * The web application will be available at http://127.0.0.1:8080
